@model IEnumerable<Application.People.Queries.GetPeopleList.PeopleListItemModel>

@{
    ViewBag.Title = "People";
}

<h2>People</h2>

@foreach (var item in Model)
{
    @Html.DisplayFor(modelItem => item.Name).ToString()
    <hr />
}